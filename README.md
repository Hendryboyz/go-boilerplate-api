# Go Boilerplate API

> Attempt to implement an todo list API follow [The 12 Factor App](https://12factor.net/)

```bash
tree -I 'allure*|node_modules|tmp|*.sh|*.out|*.sql|*.json|uploads*|test*' -L 3

├── bootstrap              # app bootstrap: HTTP server, gRPC server, config loader
│   ├── app.go
│   ├── config.go
│   ├── index.go
│   └── validator.go
├── buf.gen.yaml           # buf code generation config for protobuf
├── buf.lock
├── buf.yaml               # buf module and lint config
├── build
│   └── Dockerfile
├── cmd                    # cobra commands entry point
│   └── index.go
├── configs                # configuration provider and sample config
│   ├── config.go
│   └── local.yml
├── docker-compose.yaml
├── docs                   # swagger document generated by swag (DO NOT EDIT)
│   ├── docs.go
│   └── swagger.yaml
├── global                 # global-scoped variables
│   └── app.go
├── go.mod
├── go.sum
├── internal
│   ├── app
│   │   ├── api
│   │   │   ├── grpc       # gRPC handlers
│   │   │   ├── http       # HTTP router, middleware, controllers and DTOs
│   │   │   ├── params     # shared request parsing utilities
│   │   │   └── inject.go  # wires HTTP controllers and gRPC handlers into server providers
│   │   ├── model          # domain models (GORM structs)
│   │   ├── repository     # GORM repository implementations
│   │   ├── todo           # todo domain: service, repository interface
│   │   └── inject.go      # aggregates domain ProviderSets and interface bindings
│   ├── helpers
│   │   ├── inject.go      # wires Clock interface binding
│   │   ├── math.go
│   │   └── time.go
│   └── pkg
│       ├── cache          # Redis cache client
│       ├── db             # GORM database connection
│       ├── log            # zap logger wrapper
│       └── inject.go      # wires infrastructure clients (DB, Redis)
├── LICENSE
├── main.go
├── pkg
│   ├── proto              # generated protobuf Go code (DO NOT EDIT)
│   │   └── todo
│   └── validator.go       # custom validators for gin
├── proto                  # protobuf source definitions
│   └── todo
│       └── v1
├── README.md
├── wire_gen.go            # generated wire DI code (DO NOT EDIT)
└── wire.go                # wire DI initializer
```

## Dependencies

1. [Cobra - Command-line Interface Tools](https://github.com/spf13/cobra): build the admin command
   * [User Guideline](https://github.com/spf13/cobra/blob/main/site/content/user_guide.md)
   * Use [cobra-cli](https://github.com/spf13/cobra-cli/blob/main/README.md) to generate cobra command is more efficient
2. [Viper - Configuration](https://github.com/spf13/viper): manage different format of configuration
3. [Gin_Gonic - HTTP Server](https://github.com/gin-gonic/gin): the web framework
4. [GORM - ORM](https://gorm.io/gorm): abstract different type of relational database
5. [zap - Logging](https://github.com/uber-go/zap): logging tool
6. [Testify - Testing](https://github.com/stretchr/testify): assertion and mocking framework
7. [Allure-Go - Test Reporting](https://github.com/ozontech/allure-go): structured test reporting with Allure framework
8. [swag - OpenAPI](https://github.com/swaggo/gin-swagger): generate swagger document
   * [How to comment API document to generate document](https://github.com/swaggo/swag?tab=readme-ov-file#general-api-info)
9. [Wire - Dependency Injection](https://github.com/google/wire): compile-time dependency injection code generation
10. [gRPC](https://google.golang.org/grpc): high-performance RPC framework
11. [gRPC-Gateway](https://github.com/grpc-ecosystem/grpc-gateway): transcodes RESTful HTTP API to gRPC
12. [Protobuf](https://google.golang.org/protobuf): protocol buffers serialization
13. [Redis - go-redis](https://github.com/redis/go-redis): Redis client
14. [Azure CosmosDB SDK](https://github.com/Azure/azure-sdk-for-go/sdk/data/azcosmos): Azure Cosmos DB client
15. [Elastic APM](https://go.elastic.co/apm): application performance monitoring for HTTP, gRPC, and GORM
16. [automaxprocs](https://github.com/uber-go/automaxprocs): automatically set GOMAXPROCS to match Linux container CPU quota
17. [Hot Reload](https://github.com/air-verse/air): auto reload gin app code change to make developer's live easier
18. [Graceful Shutdown - Github Example](https://github.com/gin-gonic/examples/blob/master/graceful-shutdown/graceful-shutdown/notify-without-context/server.go)

:bulb: the order to `go get` those packages matters

## Command

### Local Development

```bash
docker-compose up -d

# please install `air` first to hot reload while code change
go install github.com/air-verse/air@latest
air
```

### Build and Run

```bash
go build -o api
api --help # check the admin command for this server
```

```bash
docker build -t my-golang-webapp:latest .
docker run --env GIN_MODE=release \
   --name api \
   --mount type=bind,src=/Users/henry.chou/sources/go-boilerplate-api/configs/local.yml,dst=/app/configs/local.yml \
   --network=host \
   -p 8081:8081 \
   my-golang-webapp:latest
docker rm $(docker container ls --filter "ancestor=my-golang-webapp" -aq | head -n 1)
```

### Generate protobuf code

```bash
# install buf if not already installed
go install github.com/bufbuild/buf/cmd/buf@latest

# lint proto files
buf lint

# generate Go code from proto definitions into pkg/proto
buf dep update
buf generate --path proto 

go mod tidy
```

### Build open api document

```bash
# generate swagger json and yaml automatically
swag init -o api

# format swagger comment
swag fmt
```

## Notes

* Fix go.mod with `go mod tidy` command